const sum = require('./Arrays/1.BasicLoop');

test('adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});


const total = require('./Arrays/goodVsEvil');

test('googToal > evilTotal total equal', () => {
  expect(total())
})